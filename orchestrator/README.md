# orchestrator

HITL orchestration + caching.

## CLI

```
python -m orchestrator.cli --theme "space" --mood funny --duration 300 --auto-approve
```

Outputs `data/episode_manifest.json` plus artifact IDs for preview/final MP4s.
